/// RateBoardKit — a white-label, offline-first currency rates core for iOS,
/// built on the free, key-less Frankfurter API (ECB reference rates).
///
/// The core grows in small, reviewable commits; see README for the roadmap.
public enum RateBoardKit {
    /// Semantic version of the core package.
    public static let version = "0.1.0"
}
